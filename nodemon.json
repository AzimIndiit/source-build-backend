npm run {
  "watch": ["src", ".env"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "src/**/*.spec.ts"],
  "exec": "tsx --enable-source-maps src/server.ts",
  "env": {
    "NODE_ENV": "development",
    "DEBUG": "app:*"
  },
  "signal": "SIGTERM",
  "delay": 1000
  
}